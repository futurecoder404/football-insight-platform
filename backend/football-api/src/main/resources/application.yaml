spring:
  application:
    name: football-api

  datasource:
    url: jdbc:postgresql://localhost:5432/football_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true